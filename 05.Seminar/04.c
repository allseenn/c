#include <stdio.h>

int grow_up(int n){
    int a = n%10
    int b = n%100
    while(n>0){

    }
}

int main()
{
    int n;
    scanf("%d", &n);
    grow_up(n) ? printf("YES") : printf("NO");
    return 0;
}
